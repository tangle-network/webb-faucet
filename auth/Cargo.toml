[package]
name = "webb-auth"
version = "0.1.0"
license-file = "LICENSE"
readme = "README.md"
edition = "2021"

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
serde = "1.0"
thiserror = "1.0"
webb-proposals = "0.5.4"
